#!/usr/bin/env bash
if ! command -v bun &>/dev/null; then
  echo "error: gh-p requires Bun (https://bun.sh). Install with: curl -fsSL https://bun.sh/install | bash" >&2
  exit 1
fi
exec bun "$(dirname "$0")/index.ts" "$@"
